<!-- Sticky Navbar -->
<nav id='navbarTop' class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Coffee Capital</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                {{#if logged_in}}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Account
                    </a>

                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="./profile">Profile</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" id="logout">Logout</a></li>
                    </ul>
                </li>
                {{else}}
                    <li class="nav-item">
                        <a class="nav-link active" href="./login">Login</a>
                    </li>
                {{/if}}
                {{#if logged_in}}
                <li class="nav-item">
                    <a class="nav-link">Welcome back!</a>
                    </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link">Make a free account!</a>
                    </li>
                {{/if}}
            </ul>
            <a href="/cart/" class="btn btn-primary" role="button">Shopping Cart{{cart.totalQty}}</a>
        </div>
    </div>
</nav>

<!-- Top Navbar -->
<nav class="navbar navbar-expand-lg">
    <div class="container">
        <img id="coffeeCapitalLogo"  href="/" src="/images/CoffeeCapitalLogoTrm.png">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav m-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Home</a>
                </li>
                {{#if logged_in}}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        Account
                    </a>

                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="./profile">Profile</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" id="logout">Logout</a></li>
                    </ul>
                </li>
                    {{else}}
                    <li class="nav-item">
                        <a class="nav-link active" href="./login">Login</a>
                    </li>
                    {{/if}}


                    {{#if logged_in}}
                    <li class="nav-item">
                        <a class="nav-link">Welcome back!</a>
                    </li>
                    {{else}}
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Make an account for free!</a>
                    </li>
                    {{/if}}
            </ul>
            <a href="/cart/" class="btn btn-primary" role="button">Shopping Cart{{cart.totalQty}}</a>
        </div>
    </div>
</nav>

<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{first_name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Create a New Order:</h3>

    <form class="form new-order-form">
      <div class="form-group">
        <label for="order-name">order name:</label>
        <input class="form-input" type="text" id="order-name" name="order-name" />
      </div>
      <div class="form-group">
        <label for="total-order">Total ($):</label>
        <input class="form-input" type="number" id="total-order" name="total-order" />
      </div>
      <div class="form-group">
        <label for="order-desc">description:</label>
        <textarea class="form-input" id="order-desc" name="order-desc"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Submit</button>
      </div>
      {{!-- <div class="submit order">
        <button type="submit" class="btn btn-primary">Submit Order</button>
      </div> --}}
    </form>
  </div>

  {{!-- {{#if order_items.length}}
  <div class="col-md-6 order-list">
    <h3>Current Order:</h3>

    {{#each order as |user or id|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/order/{{order.id}}">{{order.name}}</a></h4>
      </div>
      {{!-- <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{order.id}}">DELETE</button>
      </div> --}}
    {{!-- </div> --}}
    {{!-- {{/each}}
  </div>
  {{/if}} --}} 
</div>

<!-- Scripts -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
    crossorigin="anonymous"></script>
<script src="./js/storeFront.js"></script>
<script src="./js/logout.js"></script>
<script src="./js/profile.js"></script>
