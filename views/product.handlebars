<div class="text-center">
    <h2>{{name}}</h2>
    <p>{{description}}</p>
    <p>${{price}}</p>
    <img src="/images/Products/{{img}}" alt="{{img}}" width="200" height="200">
    <br>
    <p>
        <a href="/add-to-cart/{{id}}" class="btn btn-primary" role="button">Add to shopping cart</a>
    </p>
</div>