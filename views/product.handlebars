<div class="text-center">
    <h2>{{name}}</h2>
    <p>{{description}}</p>
    <p>{{price}}</p>
    <p>
        {{!-- <button class="btn btn-primary">Order this item</button> --}}
        <a href="/add-to-cart/{{id}}" class="btn btn-primary" role="button">Add to shopping cart</a>
    </p>
</div>